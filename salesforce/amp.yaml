specVersion: 1.0.0
integrations:
  - name: readAndWriteSalesforce
    displayName: Salesforce read and write
    provider: salesforce
    read:
      objects:
        - objectName: account
          mapToName: company
          mapToDisplayName: Company
          destination: salesforceWebhook
          requiredFields:
            - fieldName: name
            - fieldName: billingcity
              mapToName: city
              mapToDisplayName: City
            - mapToName: country
              mapToDisplayName: Country
              prompt: Which field is the country for the account?
          optionalFieldsAuto: all
          backfill:
            defaultPeriod:
              days: 7

        - objectName: contact
          destination: salesforceWebhook
          schedule: "*/10 * * * *" # Every 10 minutes
          requiredFields:
            - fieldName: name
          optionalFieldsAuto: all
          backfill:
            defaultPeriod:
              fullHistory: true

    subscribe:
      objects:
        - objectName: account
          destination: salesforceWebhook
          inheritFieldsAndMapping: true
          createEvent:
            enabled: always
          updateEvent:
            enabled: always
            requiredWatchFields:
              - name
              - city # this is a mapped field
              - billingpostalcode
            optionalWatchFieldsAuto: all
          deleteEvent:
            enabled: always
          otherEvents:
            - UNDELETE

    write:
      objects:
        - objectName: account
        - objectName: contact

    proxy:
      enabled: true
